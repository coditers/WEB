<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="client">


  <insert id="insert" parameterType="clientvo">
    <![CDATA[
      INSERT
        INTO client
      VALUES (null, #{firstName}, #{lastName}, #{email}, password(#{passwd }));
    ]]>
    <selectKey keyProperty="id" resultType="int">
      SELECT LAST_INSERT_ID()
    </selectKey>
  </insert>

  <!--need to check-->
  <select id="selectById" resultType="clientvo" parameterType="int">
    <![CDATA[
      SELECT *
        FROM client
       WHERE id=#{id }

    ]]>
  </select>

  <select id="selectByEmailPassword" resultType="clientvo" parameterType="clientvo">
    <![CDATA[
      SELECT id AS id,
             first_name AS firstName,
             last_name AS lastName,
             email AS email,
             passwd AS passwd
        FROM client
       WHERE email = #{email}
         AND passwd = PASSWORD(${passwd})
    ]]>
  </select>

</mapper>