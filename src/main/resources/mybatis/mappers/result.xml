<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="result">
    <!--보류-->
    <select id="selectByIds" parameterType="hashmap" resultType="resultvo">
    <![CDATA[
        SELECT applicant_id as applicantId,
                test_case_id as testCaseId,
                correctness,
                used_memory as usedMemory,
                running_time as runningTime
        FROM    result
        WHERE applicant_id = #{applicantId}
        AND test_case_id = #{testCaseId}
        ]]>
  </select>
</mapper>