<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="recruit">

  <!--need to check-->
  <select id="selectAll" resultType="recruitvo">
    <![CDATA[
      SELECT id        AS id,
             title     AS title,
             from_date AS fromDate,
             to_date   AS toDate,
             client_id AS clientId
        FROM recruit
    ]]>
  </select>

  <!--need to check-->
  <select id="selectListByClientId" resultType="recruitvo" parameterType="recruitvo">
    <![CDATA[
      SELECT id        AS id,
             title     AS title,
             from_date AS fromDate,
             to_date   AS toDate,
             client_id AS clientId
        FROM recruit
        WHERE client_id = #{clientId}
    ]]>
  </select>

  <insert id="insert" parameterType="recruitvo">
    <![CDATA[
      INSERT
        INTO recruit
      VALUES (NULL, '${title}', NULL, NULL, ${clientId})
    ]]>
    <selectKey keyProperty="id" resultType="int">
      SELECT LAST_INSERT_ID()
    </selectKey>
  </insert>


  <!--need to check-->
  <select id="selectById" resultType="recruitvo" parameterType="int">
    <![CDATA[
      SELECT id        AS id,
             title     AS title,
             from_date AS fromDate,
             to_date   AS toDate,
             client_id AS clientId
        FROM recruit
       WHERE id=#{id }
    ]]>
  </select>


</mapper>